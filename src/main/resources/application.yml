spring:
  config:
        activate:
            on-profile: default
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://naton-mysql.mysql:3306/companyowner
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    database: mysql
server:
  port: 9003
  
---  
spring:
    config:
        activate:
            on-profile: dev
    datasource:
        url: jdbc:h2:./test;INIT=CREATE SCHEMA IF NOT EXISTS COMPANYOWNER;MODE=MySQL;
        username: sa
        password:
        driverClassName: org.h2.Driver
hibernate.dialect: org.hibernate.dialect.H2Dialect
hibernate.hbm2ddl.auto: create-drop 